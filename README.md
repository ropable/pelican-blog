Getting started:

1. Create new virtualenv using Python 3.
2. Clone repo.
3. Run `pelican content` to generate static output.
4. Make any changes to content, regenerate and upload to web host.

Useful commands:

    pelican content
    pelican --listen --autoreload

Links:

- https://docs.getpelican.com/en/stable/content.html
